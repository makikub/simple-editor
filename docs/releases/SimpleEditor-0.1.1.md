# Simple Editor 0.1.1

Distribution refresh for Simple Editor.

- Publishes a fresh notarized ZIP under a new versioned download URL.
- Keeps the Sparkle appcast pointed at the latest signed and notarized build.
- Leaves the app functionality unchanged from 0.1.0.
